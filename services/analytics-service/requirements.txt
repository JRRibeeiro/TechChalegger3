# CORRIGIDO — o app.py importa boto3, botocore e dotenv, mas o requirements
# original só tinha flask/werkzeug/gunicorn. Resultado: a imagem builda,
# mas o container morre no import com ModuleNotFoundError: boto3.
# É por isso que o analytics-service estava comentado no docker-compose
# e nunca subiu no EKS. Depois de trocar este arquivo, REBUILD + PUSH
# da imagem é obrigatório (etapa 3 do passo a passo).
flask==2.2.5
werkzeug==2.2.3
gunicorn==20.1.0
boto3
python-dotenv==1.0.0
